---
phase: 10-evaluation-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - notebooks/01_data_exploration.ipynb
  - notebooks/02_predictor_results.ipynb
  - notebooks/03_generator_quality.ipynb
  - notebooks/04_execution_backtest.ipynb
autonomous: true
must_haves:
  truths:
    - "All 4 notebooks execute end-to-end with nbconvert without errors (using dummy/sample data where real data is absent)"
    - "Each notebook has a clear narrative: markdown cells explaining each section"
    - "notebook 04 calls compare_to_baselines() and generate_all_plots() from the evaluation module"
  artifacts:
    - path: "notebooks/01_data_exploration.ipynb"
      provides: "LOB schema exploration, feature distributions, temporal split visualization"
      min_lines: 50
    - path: "notebooks/02_predictor_results.ipynb"
      provides: "TLOB vs DeepLOB F1 comparison, confusion matrices, multi-horizon results"
      min_lines: 50
    - path: "notebooks/03_generator_quality.ipynb"
      provides: "Stylized fact tests, LOB-Bench Wasserstein distances, regime conditioning plots"
      min_lines: 50
    - path: "notebooks/04_execution_backtest.ipynb"
      provides: "Agent vs baseline IS comparison, all 6 plots rendered inline"
      min_lines: 50
  key_links:
    - from: "notebooks/04_execution_backtest.ipynb"
      to: "lob_forge/evaluation/metrics.py"
      via: "imports compute_implementation_shortfall and generate_all_plots"
      pattern: "compute_implementation_shortfall|generate_all_plots"
    - from: "notebooks/03_generator_quality.ipynb"
      to: "lob_forge/evaluation/stylized_facts.py"
      via: "imports run_all_stylized_tests"
      pattern: "run_all_stylized_tests"
---

<objective>
Create 4 Jupyter notebooks that walk through data, predictor, generator, and execution results end-to-end.

Purpose: Satisfies EVAL-03. Provides the primary interface for human exploration and presentation of results.
Output: 4 .ipynb files in notebooks/ that execute without error via nbconvert --execute.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-evaluation-polish/10-01-SUMMARY.md
@lob_forge/evaluation/__init__.py
@lob_forge/executor/evaluate.py
@lob_forge/executor/baselines.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notebooks 01 and 02 (data + predictor)</name>
  <files>notebooks/01_data_exploration.ipynb, notebooks/02_predictor_results.ipynb</files>
  <action>
Create notebooks using `nbformat` (or write JSON directly). Each notebook must have:
- A markdown cell at the top with title + description
- Section headers as markdown cells
- Code cells that import and demonstrate the module
- Graceful fallback when real data files are absent (use `if Path("data/...").exists(): ... else: print("No data — showing schema only")`)

**01_data_exploration.ipynb sections:**
1. "LOB Schema" — load `lob_forge.data.schema` (or show column layout: ask_price_1..10, ask_size_1..10, bid_price_1..10, bid_size_1..10, timestamp, trade_price, trade_size)
2. "Feature Engineering" — show `compute_all_features` signature and describe OFI, MLOFI, VPIN, microprice
3. "Temporal Splits" — show `temporal_split` and explain purge gap concept with a diagram (ASCII or matplotlib)
4. "Data Quality Checks" — show `validate_schema` usage

**02_predictor_results.ipynb sections:**
1. "Model Architecture" — show `DualAttentionTransformer`, `DeepLOB`, `LinearBaseline` with parameter counts (use `sum(p.numel() for p in model.parameters())` on CPU with dummy shapes)
2. "Training Metrics" — load wandb run or show dummy F1 table with `pd.DataFrame`
3. "Multi-Horizon F1 Comparison" — bar chart comparing TLOB vs DeepLOB vs Linear at 4 horizons (1s, 2s, 5s, 10s) using dummy values if no checkpoint
4. "Confusion Matrix" — `sklearn.metrics.confusion_matrix` on dummy 3-class predictions

All cells must be runnable without real data. Use `np.random.seed(42)` dummy arrays where needed.
  </action>
  <verify>cd /Users/virenbankapur/Downloads/LOB-Forge && jupyter nbconvert --to notebook --execute notebooks/01_data_exploration.ipynb --output /tmp/test_nb1.ipynb 2>&1 | tail -5 && jupyter nbconvert --to notebook --execute notebooks/02_predictor_results.ipynb --output /tmp/test_nb2.ipynb 2>&1 | tail -5</verify>
  <done>Both notebooks execute without errors via nbconvert. Each has 4+ sections with markdown headers.</done>
</task>

<task type="auto">
  <name>Task 2: Create notebooks 03 and 04 (generator + execution)</name>
  <files>notebooks/03_generator_quality.ipynb, notebooks/04_execution_backtest.ipynb</files>
  <action>
**03_generator_quality.ipynb sections:**
1. "Diffusion Model Architecture" — show `DiffusionModel` config, UNet1D parameter count, cosine schedule visualization (plot alpha_bar vs timestep)
2. "Stylized Fact Tests" — show `run_all_stylized_tests` signature; run on dummy `np.random.randn(1000, 40)` real and synthetic arrays; display results table
3. "LOB-Bench Metrics" — show `run_lob_bench` returning wasserstein/wd_mean and discriminator/accuracy on dummy data
4. "Regime Conditioning" — show `validate_regime_conditioning` on dummy data with regime labels

**04_execution_backtest.ipynb sections:**
1. "Environment Setup" — show `LOBExecutionEnv` config (7-action space, seq_len, cost model params)
2. "Baseline Comparison" — instantiate `TWAPBaseline`, `VWAPBaseline`, `AlmgrenChrissBaseline`, `RandomBaseline`; run `run_backtest` with mock env for 5 episodes each; show results table
3. "IS Metrics" — call `compute_implementation_shortfall` on results; show IS, IS Sharpe, slippage-vs-TWAP table
4. "Visualization" — call `generate_all_plots(comparison_dict)` and display plots inline with `IPython.display.Image`

For notebook 04, build a minimal mock env (same as in test_backtest.py) that doesn't need real LOB data.
Use `pd.DataFrame` for results tables.

After writing both, run nbconvert on all 4:
```bash
for nb in notebooks/*.ipynb; do
  jupyter nbconvert --to notebook --execute "$nb" --output /tmp/test_$(basename $nb) 2>&1 | tail -3
done
```
  </action>
  <verify>cd /Users/virenbankapur/Downloads/LOB-Forge && for nb in notebooks/*.ipynb; do jupyter nbconvert --to notebook --execute "$nb" --output /tmp/test_exec.ipynb 2>&1 | grep -E "error|Error|SUCCESS|success" | head -3; done</verify>
  <done>All 4 notebooks execute end-to-end without errors. notebook 04 renders IS metrics table and 6 plots.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `jupyter nbconvert --to notebook --execute notebooks/01_data_exploration.ipynb` exits 0
- [ ] `jupyter nbconvert --to notebook --execute notebooks/02_predictor_results.ipynb` exits 0
- [ ] `jupyter nbconvert --to notebook --execute notebooks/03_generator_quality.ipynb` exits 0
- [ ] `jupyter nbconvert --to notebook --execute notebooks/04_execution_backtest.ipynb` exits 0
</verification>

<success_criteria>
- All 4 notebooks created with markdown narrative sections
- All 4 execute end-to-end without errors using dummy data where real data absent
- notebook 04 imports and uses evaluation/metrics.py + plots.py
</success_criteria>

<output>
After completion, create `.planning/phases/10-evaluation-polish/10-03-SUMMARY.md`
</output>
