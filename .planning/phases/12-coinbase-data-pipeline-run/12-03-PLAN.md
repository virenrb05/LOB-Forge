---
phase: 12-coinbase-data-pipeline-run
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/train_all.sh
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "IS (implementation shortfall) metrics are logged to wandb after executor training"
    - "Slippage-vs-TWAP metric is logged to wandb after executor training"
    - "IS Sharpe is logged to wandb after executor training"
  artifacts:
    - path: "scripts/train_all.sh"
      provides: "wandb logging of IS and slippage in Stage 6"
      contains: "wandb.log"
  key_links:
    - from: "scripts/train_all.sh Stage 6"
      to: "lob_forge.executor.compare_to_baselines"
      via: "compare_to_baselines() result used to compute IS/slippage"
      pattern: "compare_to_baselines"
    - from: "scripts/train_all.sh Stage 6"
      to: "wandb"
      via: "wandb.init + wandb.log call"
      pattern: "wandb\\.log"
---

<objective>
Log IS (implementation shortfall), IS Sharpe, and slippage-vs-TWAP metrics to wandb in train_all.sh Stage 6.

Purpose: Phase 12 success criterion 4 requires "Real metrics (F1, IS, slippage) logged to wandb". F1 is already logged by the predictor trainer. IS and slippage are computed by compare_to_baselines() but never sent to wandb. This plan closes that gap.

Output: Stage 6 in train_all.sh calls compare_to_baselines() and logs is_mean, is_std, is_sharpe, and slippage_vs_twap to a wandb run named "executor-eval".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/train_all.sh
@lob_forge/executor/evaluate.py
@lob_forge/evaluation/metrics.py
@configs/config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wandb IS/slippage logging to train_all.sh Stage 6</name>
  <files>scripts/train_all.sh</files>
  <action>
Replace the Stage 6 inline Python block in train_all.sh with an expanded version that:

1. Calls `compare_to_baselines()` using the final executor checkpoint (`checkpoints/executor_adversarial.pt`), loading real LOB data from `DATA_DIR`.
2. Computes IS and slippage using `compute_implementation_shortfall` and `compute_slippage_vs_twap` from `lob_forge.evaluation.metrics`.
3. Initialises a wandb run with `wandb.init(project="lob-forge", job_type="executor-eval", resume="allow")` — use `settings=wandb.Settings(silent=True)` to suppress verbose output.
4. Logs a single dict to wandb:
   ```python
   {
       "executor/is_mean": ...,
       "executor/is_std": ...,
       "executor/is_sharpe": ...,
       "executor/slippage_vs_twap": ...,
       "executor/dqn_beats_twap": ...,
   }
   ```
5. Calls `wandb.finish()` after logging.
6. Wraps the wandb block in a try/except so failure to log (e.g. offline) is non-fatal — print a `[WARN]` and continue.
7. Also keep the existing `generate_all_plots()` call.

**Key constraints:**
- The executor checkpoint path is `checkpoints/executor_adversarial.pt` (final stage). Skip wandb logging (with `[WARN]`) if the checkpoint does not exist — this keeps train_all.sh usable even if Stage 5 was skipped.
- LOB data for the eval env: load `DATA_DIR/test.parquet` if it exists (Stage 2 output); fall back to `DATA_DIR/train.parquet`; fall back to None (which uses dummy data in train_agent but we need a real array — use `np.random.randn(10000, 40).astype('float32')` as final fallback).
- The LOBExecutionEnv constructor needs: `lob_data` (np.ndarray shape (N, 40)), `seq_len=50`, `inventory=100.0`, `horizon=20` — use these defaults (matching executor.yaml defaults).
- `compare_to_baselines()` signature: `compare_to_baselines(checkpoint_path, env, n_episodes=10, device="cpu")`.
- `compute_slippage_vs_twap(agent_results, twap_results)` — get results from the comparison dict: `cmp["dqn"]["results"]` and `cmp["twap"]["results"]`.
  </action>
  <verify>
    1. `bash -n scripts/train_all.sh` passes (syntax check).
    2. `grep -n "wandb" scripts/train_all.sh` shows at least 3 hits (init, log, finish).
    3. `grep -n "is_mean\|is_sharpe\|slippage_vs_twap" scripts/train_all.sh` shows all three keys present.
  </verify>
  <done>Stage 6 inline Python block logs IS, IS Sharpe, and slippage_vs_twap to wandb. Script syntax is valid. Non-fatal on missing checkpoint or wandb failure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/train_all.sh` exits 0 (syntax check)
- [ ] `grep "wandb.init\|wandb.log\|wandb.finish" scripts/train_all.sh` returns matches
- [ ] `grep "executor/is_mean\|executor/is_sharpe\|executor/slippage_vs_twap" scripts/train_all.sh` returns matches
- [ ] `ruff check lob_forge/ && black --check lob_forge/` still clean (no Python changes needed, but verify)
- [ ] `pytest tests/ -x -q` still passes (307+)
</verification>

<success_criteria>

- Task 1 complete: Stage 6 logs IS/slippage to wandb
- train_all.sh syntax valid
- wandb logging is non-fatal (try/except wrapper)
- All existing tests pass
- Phase 12 success criterion 4 met: IS and slippage logged to wandb
  </success_criteria>

<output>
After completion, create `.planning/phases/12-coinbase-data-pipeline-run/12-03-SUMMARY.md`
</output>
