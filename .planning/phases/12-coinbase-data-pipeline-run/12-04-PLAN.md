---
phase: 12-coinbase-data-pipeline-run
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-coinbase-data-pipeline-run/12-04-SUMMARY.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "train_all.sh runs end-to-end on EC2 without aborting any stage"
    - "Predictor checkpoint (best_model.pt or similar) exists after Stage 3"
    - "Real F1 scores logged to wandb from predictor training on Coinbase data"
    - "IS/slippage metrics logged to wandb from executor eval"
  artifacts:
    - path: "checkpoints/"
      provides: "Executor stage checkpoints + predictor checkpoint from Coinbase run"
    - path: "outputs/plots/"
      provides: "6 publication-ready plots generated from real data"
  key_links:
    - from: "data/train.parquet"
      to: "lob_forge.predictor.trainer"
      via: "Stage 3 train.py dispatch with +trainer=predictor"
    - from: "lob_forge.predictor"
      to: "wandb lob-forge project"
      via: "wandb.log in trainer.py when cfg.wandb.enabled=True"
---

<objective>
Run the full LOB-Forge training pipeline end-to-end on the EC2 g4dn.xlarge instance using the pre-processed Coinbase BTC-USD data.

Purpose: Close Phase 12 criteria 3 and 5 — full pipeline execution on real data with wandb metrics. The preprocessor entry point, validation call, and Stage 2 are already fixed (commits 88b1348, 58f023c). Coinbase data exists in data/ (train/val/test splits). The pipeline needs to run to completion on GPU.

Output: Completed predictor + generator + executor training on Coinbase data; F1, IS, slippage logged to wandb; 6 plots generated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/train_all.sh
@configs/data.yaml
@configs/predictor.yaml
@configs/generator.yaml
@configs/executor.yaml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Push repo to EC2 and confirm environment is ready</action>
  <instructions>
    The pipeline needs to run on the g4dn.xlarge (GPU) at 98.92.76.157. Claude cannot SSH to EC2 — you need to set this up.

    Steps:
    1. SSH into EC2:
       ssh -i /path/to/key.pem ec2-user@98.92.76.157
       (or ubuntu@, centos@ depending on AMI)

    2. On EC2, clone or rsync the repo:
       git clone &lt;your-repo-url&gt; LOB-Forge
       # or from local: rsync -av --exclude='.git' /Users/virenbankapur/Downloads/LOB-Forge/ ec2-user@98.92.76.157:~/LOB-Forge/

    3. On EC2, install dependencies:
       cd ~/LOB-Forge
       pip install -e ".[dev]"

    4. Confirm GPU is available:
       python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
       # Expected: True  Tesla T4

    5. Copy data/ directory to EC2 (it has train/val/test splits):
       rsync -av /Users/virenbankapur/Downloads/LOB-Forge/data/ ec2-user@98.92.76.157:~/LOB-Forge/data/

    6. Set up wandb on EC2:
       wandb login  # paste your API key when prompted

    7. Confirm all stages will use CUDA:
       export DEVICE=cuda
  </instructions>
  <verification>
    On EC2, verify:
    - python -c "import lob_forge; print(lob_forge.__version__)" succeeds
    - ls data/train.parquet data/val.parquet data/test.parquet — all present
    - torch.cuda.is_available() returns True
  </verification>
  <resume-signal>Type "ready" when EC2 is set up, data is synced, and wandb is logged in</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Run train_all.sh on EC2 and wait for completion</action>
  <instructions>
    On EC2, run the full pipeline (skipping Stage 1 data ingestion since data is already synced):

    cd ~/LOB-Forge
    DEVICE=cuda RECORD_DURATION=60 bash scripts/train_all.sh --skip-data 2>&1 | tee pipeline_run.log

    This will run Stages 0 (validate), 2 (preprocess — already done but idempotent), 3 (predictor), 4 (generator), 5 (executor), 6 (eval + plots).

    Expected duration on g4dn.xlarge:
    - Stage 3 (predictor): ~10-20 min with smoke-test epochs
    - Stage 4 (generator): ~10-20 min
    - Stage 5 (executor): ~5 min (500 steps/stage in smoke-test mode)
    - Stage 6 (eval): ~2-3 min

    To run in smoke-test mode (faster, 3 epochs + 500 DQN steps):
    SMOKE_TEST=1 DEVICE=cuda bash scripts/train_all.sh --skip-data 2>&1 | tee pipeline_run.log

    Monitor for errors — if any stage fails, paste the error here.

    When done, copy back outputs and checkpoints:
    rsync -av ec2-user@98.92.76.157:~/LOB-Forge/outputs/ /Users/virenbankapur/Downloads/LOB-Forge/outputs/
    rsync -av ec2-user@98.92.76.157:~/LOB-Forge/checkpoints/ /Users/virenbankapur/Downloads/LOB-Forge/checkpoints/
  </instructions>
  <verification>
    Confirm the run succeeded:
    - tail -50 pipeline_run.log shows "LOB-Forge Pipeline Complete"
    - ls checkpoints/ shows executor_adversarial.pt (updated timestamp from this run)
    - ls outputs/plots/ shows 6 PNG files
    - wandb run URL appears in the log (or check wandb.ai/lob-forge)
  </verification>
  <resume-signal>
    Paste the last 20 lines of pipeline_run.log, or type "complete" and share the wandb run URL if available.
    If a stage failed, paste the error so Claude can fix it.
  </resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pipeline_run.log shows all 6 stages completed without aborting
- [ ] At least one of: best_model.pt, predictor checkpoint, or wandb predictor run exists
- [ ] outputs/plots/ contains 6 PNG files
- [ ] wandb run exists at wandb.ai/lob-forge (or log shows wandb metrics logged)
</verification>

<success_criteria>

- All tasks completed
- Pipeline ran end-to-end on real Coinbase data on GPU
- Evidence of real metrics (F1 from predictor, IS/slippage from executor) either in wandb or in pipeline_run.log
- Phase 12 criteria 3 and 5 are now met
  </success_criteria>

<output>
After completion, create `.planning/phases/12-coinbase-data-pipeline-run/12-04-SUMMARY.md`

Include:
- EC2 instance used (instance ID, GPU)
- Smoke-test or full run?
- Stages that completed successfully
- Any F1/IS/slippage numbers captured from logs or wandb
- wandb run URL if available
- Files synced back to local
</output>
