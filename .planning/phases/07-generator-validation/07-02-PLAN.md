---
phase: 07-generator-validation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lob_forge/evaluation/stylized_facts.py
  - tests/test_stylized_facts.py
autonomous: true

must_haves:
  truths:
    - "All 6 stylized fact functions pass unit tests with synthetic data"
    - "summary_figure produces a matplotlib figure with 6 subplots"
    - "run_all_stylized_tests orchestrates all 6 tests and returns combined results"
  artifacts:
    - path: "tests/test_stylized_facts.py"
      provides: "Unit tests for all 6 stylized fact functions"
      min_lines: 150
    - path: "lob_forge/evaluation/stylized_facts.py"
      provides: "summary_figure and run_all_stylized_tests added"
      exports: ["summary_figure", "run_all_stylized_tests"]
  key_links:
    - from: "test_stylized_facts.py"
      to: "stylized_facts.py"
      via: "imports all 6 test functions"
      pattern: "from lob_forge\\.evaluation\\.stylized_facts import"
    - from: "summary_figure"
      to: "matplotlib"
      via: "2x3 grid of comparison plots"
      pattern: "plt\\.subplots"
---

<objective>
Add unit tests for all 6 stylized fact functions, plus a summary_figure function and run_all_stylized_tests orchestrator.

Purpose: Tests validate the statistical functions work correctly on known data. The summary figure (GEN-06 requirement #7) produces a publication-ready visual comparison. The orchestrator enables one-call validation.

Output: Comprehensive test file + summary_figure + run_all_stylized_tests in stylized_facts.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Must read the implemented functions from Plan 01:
@lob_forge/evaluation/stylized_facts.py

# Test patterns from existing tests:
@tests/test_generator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for all 6 stylized fact functions</name>
  <files>tests/test_stylized_facts.py</files>
  <action>
    Create test file following existing test patterns (see test_generator.py for style).

    Test strategy: Generate synthetic data with KNOWN statistical properties so tests are deterministic.
    Use np.random.default_rng(seed=42) for reproducibility.

    **TestReturnDistribution:**
    - test_identical_distributions: Same data → KS p_value near 1.0, passed=True
    - test_different_distributions: Normal vs uniform returns → p_value < 0.05, passed=False
    - test_fat_tails: Student-t(df=3) returns → kurtosis > 3

    **TestVolatilityClustering:**
    - test_clustered_returns: Generate GARCH-like process (alternate high/low vol blocks) → mean_acf > 0, passed=True
    - test_iid_returns: IID normal → mean_acf near 0

    **TestBidAskBounce:**
    - test_negative_autocorrelation: Alternating +1/-1 returns → lag1_acf < 0, passed=True
    - test_positive_autocorrelation: Trending returns → lag1_acf > 0, passed=False

    **TestSpreadCdf:**
    - test_matching_spreads: Same book data → passed=True
    - test_different_spreads: Different spread distributions → passed=False
    - Create minimal (N, 40) arrays with ask_price_1 (col 0) and bid_price_1 (col 20)

    **TestBookShape:**
    - test_matching_depth: Same book data → passed=True
    - test_different_depth: Different size profiles → low p_value

    **TestMarketImpact:**
    - test_concave_impact: Square-root impact data → beta < 1.0, passed=True
    - test_linear_impact: Linear impact → beta ≈ 1.0

    Each test class inherits from unittest.TestCase. Use setUp for shared fixtures.
  </action>
  <verify>cd /Users/virenbankapur/Downloads/LOB-Forge && python -m pytest tests/test_stylized_facts.py -v --tb=short 2>&1 | tail -20</verify>
  <done>All tests pass, covering all 6 stylized fact functions</done>
</task>

<task type="auto">
  <name>Task 2: Add summary_figure and run_all_stylized_tests to stylized_facts.py</name>
  <files>lob_forge/evaluation/stylized_facts.py</files>
  <action>
    Add two functions to stylized_facts.py:

    1. **run_all_stylized_tests(real_book, synthetic_book, real_mid, synthetic_mid) -> dict[str, dict]**
       - Calls all 6 test functions with appropriate slices of the input data
       - real_book, synthetic_book: (N, 40) arrays
       - real_mid, synthetic_mid: (N,) mid-price arrays
       - Computes returns internally from mid-price series
       - Returns dict keyed by test name → individual test result dict
       - Also adds "all_passed": bool (True if all 6 pass)

    2. **summary_figure(results: dict, real_book, synthetic_book, real_mid, synthetic_mid, save_path=None) -> matplotlib.figure.Figure**
       - Takes output of run_all_stylized_tests plus the data arrays
       - Creates 2×3 matplotlib figure with:
         - (0,0) Return distribution: overlaid histograms (real blue, synthetic orange)
         - (0,1) Volatility clustering: ACF plot of |returns| for both
         - (0,2) Bid-ask bounce: bar chart of lag-1 autocorrelation
         - (1,0) Spread CDF: empirical CDF curves for both
         - (1,1) Book shape: bar chart of mean size per level
         - (1,2) Market impact: log-log scatter + regression lines
       - Title each subplot with pass/fail from results
       - If save_path provided, save figure as PNG (300 dpi)
       - Return the Figure object (don't call plt.show())
       - Use `import matplotlib; matplotlib.use("Agg")` for headless rendering

    Update `__all__` to include both new functions.
  </action>
  <verify>python -c "from lob_forge.evaluation.stylized_facts import run_all_stylized_tests, summary_figure; print('OK')"</verify>
  <done>Both functions importable; summary_figure produces a Figure object without errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_stylized_facts.py -v` — all tests pass
- [ ] `ruff check lob_forge/evaluation/stylized_facts.py tests/test_stylized_facts.py` passes
- [ ] `black --check lob_forge/evaluation/stylized_facts.py tests/test_stylized_facts.py` passes
- [ ] `python -c "from lob_forge.evaluation.stylized_facts import run_all_stylized_tests, summary_figure"` succeeds
</verification>

<success_criteria>

- All 6 stylized fact functions have unit tests
- Tests use deterministic synthetic data (seeded RNG)
- summary_figure produces 2×3 grid with labeled subplots
- run_all_stylized_tests orchestrates all tests with one call
- All tests pass, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-generator-validation/07-02-SUMMARY.md`
</output>
