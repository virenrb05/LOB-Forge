---
phase: 01-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - configs/config.yaml
  - configs/data.yaml
  - configs/predictor.yaml
  - configs/generator.yaml
  - configs/executor.yaml
  - lob_forge/train.py
  - scripts/validate_mps.py
autonomous: true

must_haves:
  truths:
    - "python -m lob_forge.train --help displays Hydra config options"
    - "python -m lob_forge.train --cfg job prints resolved config"
    - "Hydra overrides work: python -m lob_forge.train data.symbol=ETHUSDT"
    - "MPS validation script completes float32 forward+backward pass"
  artifacts:
    - path: "configs/config.yaml"
      provides: "Root Hydra config composing all sub-configs"
      contains: "defaults:"
    - path: "configs/data.yaml"
      provides: "Data pipeline configuration"
      contains: "symbol:"
    - path: "configs/predictor.yaml"
      provides: "Predictor model configuration"
      contains: "d_model:"
    - path: "configs/generator.yaml"
      provides: "Generator model configuration"
      contains: "noise_steps:"
    - path: "configs/executor.yaml"
      provides: "Executor agent configuration"
      contains: "n_actions:"
    - path: "lob_forge/train.py"
      provides: "Hydra entry point"
      contains: "@hydra.main"
      min_lines: 15
    - path: "scripts/validate_mps.py"
      provides: "MPS backend validation"
      contains: "mps"
      min_lines: 30
  key_links:
    - from: "lob_forge/train.py"
      to: "configs/config.yaml"
      via: "Hydra decorator config_path/config_name"
      pattern: "config_path.*config_name"
    - from: "configs/config.yaml"
      to: "configs/data.yaml"
      via: "Hydra defaults list"
      pattern: "- data"
---

<objective>
Create the Hydra configuration hierarchy and MPS validation script so configs load with overrides and float32 training runs on Apple Silicon.

Purpose: Establish the config-driven training infrastructure and verify MPS hardware compatibility.
Output: Working Hydra configs, train.py entry point, and passing MPS validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-01-SUMMARY.md
@LOB-Forge_Technical_Specification.docx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hydra config hierarchy and train.py entry point</name>
  <files>configs/config.yaml, configs/data.yaml, configs/predictor.yaml, configs/generator.yaml, configs/executor.yaml, lob_forge/train.py</files>
  <action>
    1. Create `configs/config.yaml` as the root config with Hydra defaults list composing sub-configs:
       ```yaml
       defaults:
         - data: data
         - predictor: predictor
         - generator: generator
         - executor: executor
         - _self_

       project:
         name: lob-forge
         seed: 42
         device: auto  # auto-detects MPS/CUDA/CPU

       training:
         epochs: 50
         batch_size: 256
         gradient_accumulation: 2
         early_stopping_patience: 10
         num_workers: 0  # MPS works best with 0

       wandb:
         project: lob-forge
         enabled: true
       ```

    2. Create `configs/data.yaml` with data pipeline params from tech spec:
       - symbol: BTCUSDT
       - source: bybit (or lobster)
       - depth: 10 (LOB levels)
       - sequence_length: 100
       - horizons: [10, 20, 50, 100] (snapshots = 1s, 2s, 5s, 10s at 100ms)
       - label_threshold: 0.00002 (2 bps)
       - normalization_window: 5000
       - resample_interval_ms: 100
       - split: {train: 0.7, val: 0.15, test: 0.15}
       - data_dir: data/

    3. Create `configs/predictor.yaml` with model params from tech spec Section 3.2:
       - model: dual_attention (or deeplob, linear)
       - d_model: 64
       - n_heads: 4
       - n_spatial_layers: 2
       - n_temporal_layers: 2
       - feedforward_dim: 128
       - dropout: 0.1
       - n_levels: 10
       - n_classes: 3
       - focal_loss_gamma: 2.0
       - vpin_head: true
       - vpin_loss_weight: 0.1
       - optimizer: {lr: 1e-4, weight_decay: 1e-5}
       - scheduler: {max_lr: 1e-3, pct_start: 0.1}

    4. Create `configs/generator.yaml` with diffusion params from tech spec Section 4:
       - noise_steps: 1000
       - ddim_steps: 50
       - noise_schedule: cosine
       - d_model: 128
       - n_layers: 4
       - ema_decay: 0.9999
       - conditioning: [timestep, regime, time_of_day]

    5. Create `configs/executor.yaml` with RL params from tech spec:
       - n_actions: 7
       - gamma: 0.99
       - lr: 5e-4
       - buffer_size: 100000
       - batch_size: 64
       - epsilon_start: 1.0
       - epsilon_end: 0.01
       - epsilon_decay: 0.995
       - target_update_freq: 1000
       - curriculum_stages: [low_vol, mixed, adversarial]

    6. Create `lob_forge/train.py` as the Hydra entry point:
       - Import hydra, omegaconf
       - `@hydra.main(version_base=None, config_path="../configs", config_name="config")`
       - `def main(cfg: DictConfig) -> None:` that prints the resolved config (placeholder — future phases add real training logic)
       - Add `if __name__ == "__main__": main()`
       - Use proper type hints

    NOTE: Use flat config structure (not config groups in subdirectories) since we have one config per component. The defaults list references files directly in configs/.
  </action>
  <verify>python -m lob_forge.train --help shows config options AND python -m lob_forge.train --cfg job prints resolved config with all sub-configs merged</verify>
  <done>Hydra loads root config with all sub-configs, overrides work (e.g., data.symbol=ETHUSDT)</done>
</task>

<task type="auto">
  <name>Task 2: Create MPS validation script</name>
  <files>scripts/validate_mps.py</files>
  <action>
    1. Create `scripts/validate_mps.py` that validates PyTorch MPS backend:
       - Check `torch.backends.mps.is_available()` and `torch.backends.mps.is_built()`
       - If MPS not available, print warning and fall back to CPU (don't fail — CI may not have MPS)
       - Create a small test model (2-layer MLP, ~1000 params) on detected device
       - Create random input tensor on device (batch=16, features=40 matching LOB feature count)
       - Run forward pass, compute dummy loss (MSE against random target)
       - Run backward pass
       - Run optimizer step (AdamW)
       - Verify: output tensor is on correct device, gradients computed, no NaN in output or gradients
       - Print summary: device used, forward pass time, backward pass time, total time, memory allocated
       - Test float32 specifically (MPS has issues with float64)
       - Exit 0 on success, exit 1 on failure

    2. Make script executable and add type hints throughout

    3. Run `black` and `ruff` on the new files to ensure linting still passes
  </action>
  <verify>python scripts/validate_mps.py exits 0 and prints device/timing summary</verify>
  <done>MPS validation completes successfully with float32 forward+backward pass, or gracefully falls back to CPU with clear message</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m lob_forge.train --help` displays Hydra options
- [ ] `python -m lob_forge.train --cfg job` prints full resolved config
- [ ] `python -m lob_forge.train data.symbol=ETHUSDT` overrides symbol correctly
- [ ] `python scripts/validate_mps.py` exits 0
- [ ] `black --check .` still passes
- [ ] `ruff check .` still passes
</verification>

<success_criteria>
- All tasks completed
- Hydra config system fully functional with overrides
- MPS validation passes (or graceful CPU fallback)
- Linting still clean after new files added
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-02-SUMMARY.md`
</output>
