---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - lob_forge/__init__.py
  - lob_forge/data/__init__.py
  - lob_forge/data/downloader.py
  - lob_forge/data/preprocessor.py
  - lob_forge/data/dataset.py
  - lob_forge/predictor/__init__.py
  - lob_forge/predictor/spatial_attention.py
  - lob_forge/predictor/temporal_attention.py
  - lob_forge/predictor/model.py
  - lob_forge/predictor/train.py
  - lob_forge/generator/__init__.py
  - lob_forge/generator/noise_schedule.py
  - lob_forge/generator/unet.py
  - lob_forge/generator/model.py
  - lob_forge/generator/train.py
  - lob_forge/executor/__init__.py
  - lob_forge/executor/environment.py
  - lob_forge/executor/agent.py
  - lob_forge/executor/train.py
  - lob_forge/evaluation/__init__.py
  - lob_forge/evaluation/backtest.py
  - lob_forge/evaluation/stylized_facts.py
  - lob_forge/evaluation/metrics.py
  - lob_forge/utils/__init__.py
  - lob_forge/utils/logging.py
  - lob_forge/utils/visualization.py
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "python -c 'import lob_forge' succeeds after pip install -e ."
    - "black --check . && ruff check . pass on entire codebase"
    - "All submodules importable: lob_forge.data, lob_forge.predictor, lob_forge.generator, lob_forge.executor, lob_forge.evaluation, lob_forge.utils"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package definition with all dependencies and tool configs"
      contains: "name = \"lob-forge\""
    - path: "lob_forge/__init__.py"
      provides: "Package root with version"
    - path: ".gitignore"
      provides: "Git ignore for Python/ML project"
  key_links:
    - from: "pyproject.toml"
      to: "lob_forge/"
      via: "packages declaration"
      pattern: "lob_forge"
---

<objective>
Create the full Python package structure with all module stubs, pyproject.toml with dependencies, and linting configuration that passes on the entire codebase.

Purpose: Establish the installable package skeleton that all future phases build on.
Output: Installable `lob_forge` package with passing black + ruff checks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@LOB-Forge_Technical_Specification.docx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package structure with pyproject.toml and all module stubs</name>
  <files>pyproject.toml, .gitignore, lob_forge/__init__.py, lob_forge/data/__init__.py, lob_forge/data/downloader.py, lob_forge/data/preprocessor.py, lob_forge/data/dataset.py, lob_forge/predictor/__init__.py, lob_forge/predictor/spatial_attention.py, lob_forge/predictor/temporal_attention.py, lob_forge/predictor/model.py, lob_forge/predictor/train.py, lob_forge/generator/__init__.py, lob_forge/generator/noise_schedule.py, lob_forge/generator/unet.py, lob_forge/generator/model.py, lob_forge/generator/train.py, lob_forge/executor/__init__.py, lob_forge/executor/environment.py, lob_forge/executor/agent.py, lob_forge/executor/train.py, lob_forge/evaluation/__init__.py, lob_forge/evaluation/backtest.py, lob_forge/evaluation/stylized_facts.py, lob_forge/evaluation/metrics.py, lob_forge/utils/__init__.py, lob_forge/utils/logging.py, lob_forge/utils/visualization.py</files>
  <action>
    1. Create `pyproject.toml` with:
       - `[project]` section: name="lob-forge", version="0.1.0", requires-python=">=3.10"
       - Dependencies: torch>=2.1.0, hydra-core>=1.3, omegaconf>=2.3, wandb, pyarrow, pandas, numpy, scipy, matplotlib, seaborn, gymnasium, tqdm
       - Dev dependencies group: black, ruff, pytest, ipykernel
       - `[build-system]`: setuptools>=68.0, setuptools-scm
       - `[tool.black]`: line-length=88
       - `[tool.ruff]`: line-length=88, target-version="py310"
       - `[tool.ruff.lint]`: select=["E","F","W","I","UP","B","SIM"], ignore=["E501"] (line length handled by black)
       - `[tool.pytest.ini_options]`: testpaths=["tests"]

    2. Create `lob_forge/__init__.py` with `__version__ = "0.1.0"` and a docstring.

    3. Create all submodule `__init__.py` files (data, predictor, generator, executor, evaluation, utils) — each with a brief module docstring, nothing else.

    4. Create all stub `.py` files listed in the tech spec directory structure. Each stub should contain:
       - A module-level docstring describing what it will implement (one sentence)
       - Type hints on any placeholder classes/functions are NOT needed yet — just the docstring
       - Do NOT create placeholder classes or functions — just the docstring

    5. Create `.gitignore` for Python/ML project: __pycache__, *.pyc, .eggs, *.egg-info, dist/, build/, .venv/, venv/, *.pt, *.pth, *.ckpt, data/, outputs/, wandb/, .env, *.parquet (large data), notebooks/.ipynb_checkpoints/

    6. Create empty directories that need to exist: tests/, notebooks/, scripts/, configs/

    7. Install package in editable mode: `pip install -e ".[dev]"`
  </action>
  <verify>python -c "import lob_forge; print(lob_forge.__version__)" prints "0.1.0"</verify>
  <done>Package installs and all submodules are importable</done>
</task>

<task type="auto">
  <name>Task 2: Verify linting passes on entire codebase</name>
  <files>pyproject.toml (ruff/black config already added in Task 1)</files>
  <action>
    1. Run `black --check .` — if it fails, run `black .` to auto-format, then verify with `--check`
    2. Run `ruff check .` — if it fails, run `ruff check --fix .` for auto-fixable issues, then manually fix remaining
    3. Iterate until both pass cleanly
  </action>
  <verify>black --check . && ruff check . both exit 0</verify>
  <done>All Python files pass black formatting and ruff linting with zero violations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -e ".[dev]"` succeeds
- [ ] `python -c "import lob_forge"` succeeds
- [ ] `python -c "from lob_forge import data, predictor, generator, executor, evaluation, utils"` succeeds
- [ ] `black --check .` exits 0
- [ ] `ruff check .` exits 0
</verification>

<success_criteria>
- All tasks completed
- Package installable in editable mode
- All submodules importable
- Linting passes with zero violations
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`
</output>
