---
phase: 03-data-preprocessing
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified: [tests/test_preprocessor.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "rolling_zscore output has approximately zero mean per feature (within statistical tolerance)"
    - "rolling_zscore output has approximately unit variance per feature (within tolerance)"
    - "rolling_zscore is strictly causal — changing a future value does not affect past normalized values"
    - "rolling_zscore handles edge cases: constant series, single row, NaN values"
  artifacts:
    - path: "tests/test_preprocessor.py"
      provides: "Unit tests for rolling_zscore function"
      min_lines: 60
  key_links:
    - from: "tests/test_preprocessor.py"
      to: "lob_forge/data/preprocessor.py"
      via: "imports rolling_zscore"
      pattern: "from lob_forge\\.data\\.preprocessor import rolling_zscore"
---

<objective>
Add comprehensive unit tests for the `rolling_zscore()` function in `lob_forge/data/preprocessor.py`.

Purpose: The function exists and is correct by inspection but has zero direct test coverage. This gap was identified by phase verification. TDD-style: write tests that exercise the function's contract.
Output: Test file with tests covering zero-mean/unit-variance output, causality, and edge cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lob_forge/data/preprocessor.py
@lob_forge/data/schema.py
@tests/test_features.py
</context>

<feature>
  <name>Unit tests for rolling_zscore()</name>
  <files>tests/test_preprocessor.py, lob_forge/data/preprocessor.py</files>
  <behavior>
    Tests to write (the function already exists, so RED phase = writing tests that pass against existing code):

    1. **Zero-mean property**: Generate a random DataFrame with 1000+ rows, apply rolling_zscore with window=100. After the warm-up period (first `window` rows), the normalized values should have mean close to 0 (abs(mean) < 0.1).

    2. **Unit-variance property**: Same setup. After warm-up, rolling std of normalized values should be close to 1.0 (within tolerance).

    3. **Causality test**: Create DataFrame, apply rolling_zscore. Mutate a value at row 500. Re-apply rolling_zscore. Assert rows 0..499 are identical in both outputs. Rows >= 500 may differ.

    4. **Stats DataFrame returned**: Verify the second return value contains `{col}_mean` and `{col}_std` columns for each input feature column.

    5. **Constant series**: All values identical → normalized values should be 0.0 (since std replaced with 1.0, and x - mean = 0).

    6. **Single row**: DataFrame with 1 row → should not crash, returns valid output.

    7. **Window parameter**: Different window sizes produce different normalizations (window=10 vs window=1000 on same data should differ).
  </behavior>
  <implementation>
    The function already exists — this is a test-only plan. Write tests in `tests/test_preprocessor.py`.
    Follow existing test patterns from `tests/test_features.py` (pytest classes, descriptive names).
    Use `np.random.default_rng(42)` for reproducibility.
    Import `rolling_zscore` from `lob_forge.data.preprocessor`.
  </implementation>
</feature>

<verification>
- [ ] `pytest tests/test_preprocessor.py -v` — all tests pass
- [ ] At least 6 test cases covering the properties listed above
- [ ] No modifications to `lob_forge/data/preprocessor.py` (tests only)
</verification>

<success_criteria>
- All tests pass against existing rolling_zscore implementation
- Zero-mean, unit-variance, and causality properties verified
- Edge cases (constant, single row) covered
- Stats return value validated
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-preprocessing/03-06-SUMMARY.md`
</output>
