---
phase: 11-fix-dispatch-lint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lob_forge/evaluation/__init__.py
  - lob_forge/evaluation/backtest.py
  - tests/test_eval_metrics.py
  - lob_forge/**/*.py
autonomous: true
must_haves:
  truths:
    - "black --check . passes with 0 files reformatted"
    - "ruff check . passes with 0 errors"
    - "pytest tests/ -q passes after lint fixes (no regressions)"
  artifacts:
    - path: "lob_forge/evaluation/__init__.py"
      provides: "isort-sorted imports (ruff I001 fixed)"
    - path: "lob_forge/evaluation/backtest.py"
      provides: "black-formatted file"
    - path: "tests/test_eval_metrics.py"
      provides: "black-formatted file"
---

<objective>
Fix all black and ruff violations across the entire codebase so `black --check . && ruff check .` passes cleanly.

Purpose: The E2E pipeline requires clean linting (EVAL-06). Phase 10 introduced 2 black violations and 34 ruff errors concentrated in evaluation/ and test files.

Output: All Python files formatted and import-sorted; clean lint pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-fix all ruff and black violations</name>
  <files>lob_forge/evaluation/__init__.py, lob_forge/evaluation/backtest.py, tests/test_eval_metrics.py</files>
  <action>
    Run auto-fix tools in sequence from the project root (/Users/virenbankapur/Downloads/LOB-Forge):

    1. Fix ruff auto-fixable errors (includes import sorting I001, unused imports F401, etc.):
       `python -m ruff check . --fix`

    2. Apply ruff unsafe fixes if needed:
       `python -m ruff check . --fix --unsafe-fixes`

    3. Format with black:
       `python -m black .`

    4. Run ruff again to catch any issues introduced by black reformatting:
       `python -m ruff check . --fix`

    5. Run black check to confirm clean:
       `python -m black --check .`

    6. Run ruff check to confirm zero errors:
       `python -m ruff check .`

    If any errors remain after auto-fix (ruff cannot fix them automatically), manually inspect and fix:
    - E501 line-too-long: break long strings/comments
    - Manual import organization if isort can't auto-fix

    Do NOT change any logic — only formatting, import ordering, and style fixes.
    Do NOT run `git add .` — stage files individually after verification.
  </action>
  <verify>python -m black --check . 2>&1 | tail -3 && python -m ruff check . 2>&1 | tail -3</verify>
  <done>black --check . exits 0 (no files to reformat); ruff check . exits 0 (no errors found)</done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions after lint fixes</name>
  <files></files>
  <action>
    Run the full test suite to confirm lint fixes didn't break any logic:
    `python -m pytest tests/ -q --tb=short`

    Expected: same number of passing tests as before (307 passed). Any failures mean a lint fix accidentally changed logic — investigate and revert that specific change.

    Also verify the package still imports cleanly:
    `python -c "import lob_forge; from lob_forge.evaluation import compute_implementation_shortfall, generate_all_plots; print('imports ok')" `
  </action>
  <verify>python -m pytest tests/ -q --tb=short 2>&1 | tail -5</verify>
  <done>307+ tests passing, 0 failures; package imports clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m black --check .` exits 0 with "All done!" or "1 file left unchanged" — no reformats needed
- [ ] `python -m ruff check .` exits 0 with no output (or "All checks passed!")
- [ ] `python -m pytest tests/ -q` still shows 307+ passed, 0 failed
- [ ] `python -c "import lob_forge"` succeeds
</verification>

<success_criteria>
- All tasks completed
- black and ruff both pass cleanly
- No test regressions
- Only formatting changes committed (no logic changes)
</success_criteria>

<output>
After completion, create `.planning/phases/11-fix-dispatch-lint/11-02-SUMMARY.md`
</output>
