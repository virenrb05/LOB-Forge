---
phase: 11-fix-dispatch-lint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lob_forge/train.py
  - scripts/train_all.sh
autonomous: true
must_haves:
  truths:
    - "python -m lob_forge.train --config-name generator trainer=generator runs train_generator() not train_model()"
    - "train_all.sh Stage 4 successfully dispatches to generator training (python -m lob_forge.train --config-name generator trainer=generator)"
    - "python -m lob_forge.train --help still works (Hydra help is not broken)"
  artifacts:
    - path: "lob_forge/train.py"
      provides: "Hydra entry point that dispatches to predictor or generator trainer based on cfg.trainer key"
      contains: "train_generator"
    - path: "scripts/train_all.sh"
      provides: "Stage 4 invocation of generator training via train.py dispatch"
      contains: "trainer=generator"
---

<objective>
Fix lob_forge/train.py so it dispatches to the generator trainer when `trainer=generator` is passed, and verify train_all.sh Stage 4 invokes it correctly.

Purpose: The E2E pipeline (train_all.sh) currently fails at Stage 4 because train.py always calls train_model() regardless of config. This breaks the reproducible pipeline goal.

Output: train.py with dispatch logic; train_all.sh Stage 4 unchanged (it already passes `trainer=generator`) but verified to work.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lob_forge/train.py
@lob_forge/generator/train.py
@scripts/train_all.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dispatch logic to lob_forge/train.py</name>
  <files>lob_forge/train.py</files>
  <action>
    Modify `main(cfg)` in `lob_forge/train.py` to dispatch based on `cfg.trainer` (or `OmegaConf.select(cfg, "trainer", default="predictor")`):

    - If `cfg.trainer == "generator"`:
      - Import `from lob_forge.generator.train import train_generator`
      - Call `train_generator(cfg)` and return
      - No data path validation (generator uses its own data loading from cfg.data.data_dir)
    - Else (default, predictor):
      - Existing train_model() path unchanged

    The `trainer` key is passed as a Hydra override: `trainer=generator`. It is NOT defined in config.yaml — it's an ad-hoc override, so use `OmegaConf.select(cfg, "trainer", default="predictor")` to avoid KeyError.

    Do NOT add `trainer` to config.yaml — it's intentionally passed only at CLI invocation time.

    Keep the existing `@hydra.main(version_base=None, config_path="../configs", config_name="config")` decorator unchanged. The `--config-name generator` flag selects generator.yaml as primary config; `trainer=generator` is the dispatch key.

    Verification: `python -m lob_forge.train trainer=generator --cfg job 2>&1 | grep -i "generator\|dispatch"` should show generator path; `python -m lob_forge.train --help` should still work.
  </action>
  <verify>python -c "from lob_forge.train import main; print('import ok')" && python -m lob_forge.train --help 2>&1 | grep -i "hydra\|help\|usage" | head -3</verify>
  <done>train.py imports cleanly; --help works; dispatch logic present (train_generator imported and called when trainer=generator)</done>
</task>

<task type="auto">
  <name>Task 2: Verify Stage 4 dispatch and run smoke test</name>
  <files>scripts/train_all.sh</files>
  <action>
    1. Read scripts/train_all.sh to confirm Stage 4 already passes `trainer=generator`. It should contain:
       `python -m lob_forge.train --config-name generator trainer=generator device="$DEVICE"`

    2. Run a dispatch smoke test (not full training — just verify routing):
       ```
       python -m lob_forge.train trainer=generator --cfg job 2>&1 | head -20
       ```
       This should show the generator config being loaded (diffusion model keys) rather than predictor keys.

    3. If train_all.sh Stage 4 line is correct (already passes trainer=generator), no change needed — just document it's verified.

    4. If Stage 4 is missing `trainer=generator`, add it.

    The goal is to confirm the CLI flag combination `--config-name generator trainer=generator` correctly routes to train_generator() and does not crash on import or dispatch.
  </action>
  <verify>python -m lob_forge.train trainer=generator --cfg job 2>&1 | grep -E "generator|diffusion|unet|noise" | head -5</verify>
  <done>Stage 4 CLI invocation routes to train_generator(); config shows generator keys (not predictor keys); no AttributeError or KeyError on dispatch</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from lob_forge.train import main"` succeeds (no ImportError)
- [ ] `python -m lob_forge.train --help` works (Hydra help not broken)
- [ ] `python -m lob_forge.train trainer=generator --cfg job` shows generator config keys
- [ ] `grep "trainer=generator" scripts/train_all.sh` confirms Stage 4 is correctly wired
</verification>

<success_criteria>
- All tasks completed
- train.py dispatches correctly by trainer key
- Stage 4 CLI invocation confirmed working
- No regressions to predictor training path
</success_criteria>

<output>
After completion, create `.planning/phases/11-fix-dispatch-lint/11-01-SUMMARY.md`
</output>
